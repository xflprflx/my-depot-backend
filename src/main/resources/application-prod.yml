spring:
  datasource:
    #TODO #PROD ajustar database path
    url: jdbc:postgresql://localhost:5432/almox_one_dev
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    #br.com.xflprflx: WARN